server {

  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ =404;
  }

  location /metrics {
    default_type text/plain;
    return 200 "frontend_up 1\n";
  }

}